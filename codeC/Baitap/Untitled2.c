#include <stdio.h>
int main()
{
    /*int n;
    printf ("Nhap vao so n: ");
    scanf( "%d", &n);
    int tong = 0;
    for ( int i=0 ; i<n; i++)
    {
        tong = tong +i;
        printf("Tong cac so o vong lap thu %d la %d ", i, tong);
        printf ("\n");
    }
    int n;
    int a[10]={1,2,6,4,3,1};
    for (int i=0; i<n; i++)
        printf ("Mang A truoc khi sap xep \n");
        printf ("%d", a[i]);
    for(int i=0; i<n; i++)
    {
        for (int j=i+1; j<n; j++)
            if a[i]> a[j]
        {
            temp = a[i];
            a[i]=a[j];
            a[j]=temp;
        }
    }
    for (int i=0; i<n; i++)
        printf ("Mang A sau khi sap xep \n");
        printf ("%d", a[i]);


    int STC = 0;
    while( STC < 120 )
    {
        printf("\n Van la sinh vien KTQD \n");
        STC = STC + 7;
        printf ("\n So Tin chi da hoc cua ky nay la %d", STC);
        while(STC < 120);*/



    /* int STC = 0;
    int soky = 0;
    while (soky<=8);
    {
        soky++;
        STC = STC +10;
        printf ("\n So Tin chi da hoc cua ky nay la %d", STC);

    } */

    printf ("Moi ban nhap vao mot ki tu tu ban phim:");
    scanf
    return 0;

}
